spring:
    cloud:
        config:
            discovery:
                enabled: true
            server:
                git:
                    uri: http://github.com/leqcar/sampleConfig

eureka:
    client:
        serviceUrl:
            defaultZone: http://discovery:8761/eureka/
server:
    port: 8888